{
    "name": "dev",
    "build": { "dockerfile": "Dockerfile", "args": { "USERNAME": "${localEnv:USER}" } },
    "mounts": [
        "source=${localEnv:HOME}/.aws,target=/home/${localEnv:USER}/.aws,type=bind,consistency=cached"
    ]
}
